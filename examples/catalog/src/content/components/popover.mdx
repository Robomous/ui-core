---
title: Popover
description: A small surface anchored to its trigger, for a short task like renaming something in place. It has no exit animation, because the trigger is a button the reader can press again on the next frame.
builtOn:
  - name: Radix Popover
    docsUrl: https://www.radix-ui.com/primitives/docs/components/popover
    apiUrl: https://www.radix-ui.com/primitives/docs/components/popover#api-reference
---

import Anchor from "@/demos/popover/anchor";
import Rename from "@/demos/popover/rename";

<Preview demo="popover/rename">
  <Rename client:load />
</Preview>

## Usage

```tsx
import {
  Button,
  Input,
  Popover,
  PopoverContent,
  PopoverDescription,
  PopoverHeader,
  PopoverTitle,
  PopoverTrigger,
} from "@robomous/ui-core";
```

```tsx
<Popover>
  <PopoverTrigger asChild>
    <Button variant="outline">Rename batch</Button>
  </PopoverTrigger>
  <PopoverContent>
    <PopoverHeader>
      <PopoverTitle>Rename</PopoverTitle>
      <PopoverDescription>Shown wherever this batch is listed.</PopoverDescription>
    </PopoverHeader>
    <Input defaultValue="batch-0012" aria-label="Batch name" />
    <Button size="sm">Save</Button>
  </PopoverContent>
</Popover>
```

`PopoverHeader` is optional layout for `PopoverTitle` and `PopoverDescription`; the rest of
`PopoverContent` is free-form, same as filling out a small form.

## Composition

```text
Popover
├─ PopoverTrigger
├─ PopoverAnchor
└─ PopoverContent
   └─ PopoverHeader
      ├─ PopoverTitle
      └─ PopoverDescription
```

## Rename

`PopoverContent` carries no exit animation, unlike a Dialog's or a Sheet's, because its trigger
is a plain button the reader is free to press again on the very next frame (see DESIGN.md,
*Motion*).

<Preview demo="popover/rename">
  <Rename client:load />
</Preview>

## Anchor

`PopoverAnchor` positions the content against a different element than the one that opens it —
here, the camera's name rather than the info button beside it.

<Preview demo="popover/anchor">
  <Anchor client:load />
</Preview>
