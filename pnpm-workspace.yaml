# The package is the repository root. The catalog is a second workspace member
# that depends on it as `workspace:*`, so it imports the real package through
# the real exports map rather than an alias into src/.
packages:
  - examples/catalog
