{
  // What ships: `src` only, declarations included. Tests live outside `src`,
  // so nothing here has to be excluded.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}
